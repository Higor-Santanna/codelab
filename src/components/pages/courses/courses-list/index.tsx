type CoursesListProps = {
    query: string;
    tags: string[] | string;
};

export const CoursesList = ({query, tags}: CoursesListProps) => {
    return(
        <section>
            
        </section>
    )
}